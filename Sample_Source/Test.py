

if abc == None or \
    len(abc) < 1:

    ;

else:

    ;
if def != None and \
    len(def) > 0:

    def = None;

else:

;

def funcGHI(self, cxparm=None):
"""..."""

    try:

;

    except Exception, inst:

        print "%s - 'funcGHI()' - exception occured..." % (self.sClassDisp);
        print type(inst);
        print inst;

        excType, excValue, excTraceback = sys.exc_info();
        asTracebackLines = traceback.format_exception(excType, excValue, excTraceback);

        print "- - - ";
        print '\n'.join(asTracebackLines);
        print "- - - ";

        return;

    return;

def JKL(self, 123=None):
"""..."""

    try:

;

    except Exception, inst:

        print "%s - 'JKL()' - exception occured..." % (self.sClassDisp);
        print type(inst);
        print inst;

        excType, excValue, excTraceback = sys.exc_info();
        asTracebackLines = traceback.format_exception(excType, excValue, excTraceback);

        print "- - - ";
        print '\n'.join(asTracebackLines);
        print "- - - ";

        return;

    return;

    def MNO(self, 345=None):
    """..."""

        try:

;

        except Exception, inst:

            print "%s - 'MNO()' - exception occured..." % (self.sClassDisp);
            print type(inst);
            print inst;

            excType, excValue, excTraceback = sys.exc_info();
            asTracebackLines = traceback.format_exception(excType, excValue, excTraceback);

            print "- - - ";
            print '\n'.join(asTracebackLines);
            print "- - - ";

            return;

        return;

    def PQR(self, 789=None):
    """Description of Method."""

        try:

            ;

        except Exception, inst:

            print "%s - 'PQR()' - exception occured..." % (self.sClassDisp);
            print type(inst);
            print inst;

            excType, excValue, excTraceback = sys.exc_info();
            asTracebackLines = traceback.format_exception(excType, excValue, excTraceback);

            print "- - - ";
            print '\n'.join(asTracebackLines);
            print "- - - ";

            return;

        return;

    if (%\c){
   %\i// Comment goes here
   %\i%\m sur_text%
   }

    def STU(self, 567=None):
    """..."""

        try:

;

        except Exception, inst:

            print "%s - 'STU()' - exception occured..." % (self.sClassDisp);
            print type(inst);
            print inst;

            excType, excValue, excTraceback = sys.exc_info();
            asTracebackLines = traceback.format_exception(excType, excValue, excTraceback);

            print "- - - ";
            print '\n'.join(asTracebackLines);
            print "- - - ";

            return;

        return;

    def VWX(self, 456=None):
    """..."""

        try:

            pass;

        except Exception, inst:

            print "%s - 'VWX()' - exception occured..." % (self.sClassDisp);
            print type(inst);
            print inst;

            excType, excValue, excTraceback = sys.exc_info();
            asTracebackLines = traceback.format_exception(excType, excValue, excTraceback);

            print "- - - ";
            print '\n'.join(asTracebackLines);
            print "- - - ";

            return;

        return;
